import { Inter, Lora } from 'next/font/google'

// inter font
export const inter = Inter({
    subsets: ['latin'],
    display: 'swap',
    variable: '--font-inter'
});

// lora font
export const lora = Lora({
    subsets: ['latin'],
    display: 'swap',
    variable: '--font-lora'
});